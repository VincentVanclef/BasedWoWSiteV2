const config = {
  auth: {
    connectionLimit: 10,
    host: "localhost",
    user: "root",
    password: "root",
    database: "auth",
    port: 3306
  },
  website: {
    connectionLimit: 10,
    host: "localhost",
    user: "root",
    password: "root",
    database: "website",
    port: 3306
  },

  // Paypal Settings
  HOME_URL: "",
  RETURN_URL: "donate/success",
  CANCEL_URL: "cancel",
  CLIENT_ID: "KEY",
  SECRET_ID: "KEY"
};

module.exports = config;
